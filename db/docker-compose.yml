version: "3.8"

services:
  postgres-rednorte-dev:
    image: postgres:16-alpine
    container_name: rednorte-postgres-dev
    restart: unless-stopped

    environment:
      # Mapeamos tus variables de entorno al contenedor de Postgres
      POSTGRES_USER: ${MYSQL_USER}
      POSTGRES_PASSWORD: ${MYSQL_PASSWORD}
      POSTGRES_DB: ${MYSQL_DATABASE}
      TZ: America/Santiago

    ports:
      # Expone el puerto 3307 de tu máquina hacia el 5432 del contenedor
      - "${DB_PORT}:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql

    networks:
      - rednorte-network

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${MYSQL_USER} -d ${MYSQL_DATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  rednorte-network:
    driver: bridge

volumes:
  postgres_data: