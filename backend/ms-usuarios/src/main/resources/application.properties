spring.application.name=ms-usuarios
server.port=8004

# Importar variables de entorno desde el archivo .env
spring.config.import=optional:file:../../../.env[.properties],optional:file:../../.env[.properties],optional:file:../.env[.properties],optional:file:.env[.properties]


# ===============================
# PERFIL ACTIVO
# ===============================
spring.profiles.active=dev

# ===============================
# ENCODING
# ===============================
spring.servlet.encoding.charset=UTF-8
spring.servlet.encoding.enabled=true
spring.servlet.encoding.force=true

# ===============================
# TIMEZONE
# ===============================
spring.jackson.time-zone=America/Santiago

# ===============================
# LOGGING
# ===============================
logging.level.root=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# ===============================
# BASE DE DATOS - POSTGRESQL
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bd_rednorte}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgrespassword}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# ===============================
# JPA / HIBERNATE
# ===============================
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Estrategias de nombres (respeta @Column exacto)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

# ===============================
# INTEGRACION - BDSISPRO
# ===============================
integration.bdsispro.url=http://localhost:8004

# ===============================
# INTEGRACION - ms-centros (Feign: CentroClient, EspecialidadClient)
# Antes el default hardcodeado apuntaba al puerto 8001 (ms-ficha-clinica,
# no ms-centros) y nunca se sobreescribía en ningún profile ni en
# docker-compose, así que estas llamadas nunca alcanzaban el servicio
# correcto.
# ===============================
feign.client.config.ms-centros.url=${MS_CENTROS_URI:http://localhost:8006}

# ===============================
# FLYWAY (solo datos de poblado; el esquema lo crea Hibernate ddl-auto)
# Deshabilitado el autoconfigurado de Spring Boot porque este correría ANTES
# que Hibernate cree las tablas. FlywaySeedRunner lo dispara manualmente
# despues del arranque, ver cl.rednorte.ms_usuarios.config.FlywaySeedRunner.
# ===============================
spring.flyway.enabled=false
app.db.auto-seed=${DB_AUTO_SEED:true}

# SWAGGER / OPENAPI
# http://localhost:8004/swagger-ui/index.html
